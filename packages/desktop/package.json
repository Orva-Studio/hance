{
  "name": "@hance/desktop",
  "version": "0.8.3",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd ../.. build:ui && electrobun dev",
    "dev:watch": "bun run --cwd ../.. build:ui && electrobun dev --watch",
    "start": "electrobun dev",
    "build": "electrobun build"
  },
  "dependencies": {
    "@hance/ui": "workspace:*",
    "electrobun": "^1.18.1"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
